[project]
name = "ml-inference-eval-platform"
version = "0.1.0"
description = "Foundation Model Inference & Evaluation Platform"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
